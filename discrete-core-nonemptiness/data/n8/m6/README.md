# Eight voters, six fractional columns

This directory contains eight-row (`n=8`) records with six fractional columns
(`m=6`). The expected exact replay has 168 hard cells, 163 fixed certificates,
five adaptive certificates, and no uncovered record.

